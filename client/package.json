{
  "name": "rustcraft-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:wasm": "cargo build --release --target wasm32-unknown-unknown --manifest-path ../mesher-wasm/Cargo.toml && cp ../mesher-wasm/target/wasm32-unknown-unknown/release/mesher_wasm.wasm src/mesher.wasm",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.1.4"
  }
}
